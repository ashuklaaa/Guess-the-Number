# Guess-the-Number
The program generates a random number within a specified range (e.g., 1 to 100), and the user is prompted to guess the number. The program provides hints after each guess, telling the user if their guess was too high or too low, until the correct number is guessed.
